"""
Пакет app.calendar — модуль работы с производственными календарями в WorkCalendarClient.


Назначение:
- организация взаимодействия с источниками календарных данных (локальными и удалёнными);
- получение и обработка информации о рабочих, праздничных и перенесённых днях;
- расчёт календарных метрик (количество рабочих дней в периоде, сдвиги дат и т. п.).

Структура пакета (текущая):
- settings.py     — базовые настройки календаря (диапазон лет, список источников данных);
- services.py   — бизнес‑логика работы с календарными данными (сервисный слой);
- validators.py — функции валидации входных параметров запросов.

Экспортируемые объекты:
- CalendarSettings   — класс настроек календаря, содержит константы конфигурации;
- calendar_service — экземпляр сервиса для работы с календарными данными.


Использование:
    from app.calendar import CalendarSettings, calendar_service


    # Чтение настроек
    min_year = CalendarSettings.MIN_YEAR
    sources = CalendarSettings.SOURCES

    # Вызов сервиса
    data = calendar_service.get_calendar_data(source='official', year=2025)

Требования:
- наличие модуля settings.py с классом CalendarSettings;
- наличие модуля services.py с объектом calendar_service.

Рекомендации по развитию:
- новые функциональные модули добавлять по принципу «одна ответственность»;
- логику работы с конкретными источниками данных изолировать в отдельные модули;
- настройки (CalendarSettings) должны быть доступны глобально для согласованности конфигурации.

Примечания:
- Пакет спроектирован для постепенного расширения: новые модули включаются по мере роста функционала;
- экспорт объектов через __all__ обеспечивает явный интерфейс пакета.
"""

from .settings import CalendarSettings
from .services import calendar_service

__all__ = ['CalendarSettings', 'calendar_service']
