"""
Пакет WorkCalendarClient.app.calendar

Предназначен для организации работы с:
- источниками производственных календарей (локальные/удалённые);
- получением и обработкой данных о рабочих/праздничных днях;
- расчётом календарных показателей (количество рабочих дней, переносы и т. д.).

Структура пакета:
- settings.py — базовые настройки календаря (диапазон лет, источники данных);
- (далее могут быть добавлены модули для:
  - загрузки данных из разных источников;
  - валидации календарных записей;
  - расчёта метрик по периодам;
  - кэширования результатов).

Экспортируемые объекты:
- CalendarSettings — класс настроек календаря, доступный извне пакета.

Использование:
    from app.calendar import CalendarSettings
    print(CalendarSettings.MIN_YEAR)
    print(CalendarSettings.SOURCES)

Требования:
- наличие модуля settings.py в пакете app.calendar;

Примечания:
- Пакет спроектирован для постепенного расширения: новые модули добавляются по мере роста функционала;
- Рекомендуется изолировать логику работы с источниками данных в отдельные модули;
- Настройки (CalendarSettings) должны быть доступны глобально для согласованности конфигурации.
"""

from .settings import CalendarSettings
from .services import calendar_service

__all__ = ['CalendarSettings', 'calendar_service']
