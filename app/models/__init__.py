"""
Модуль app.models.__init__.py — точка входа в пакет моделей WorkCalendarClient.

Объединяет и экспортирует модели данных приложения для удобного импорта
в других модулях проекта.

Назначение:
- объявляет директорию app/models Python‑пакетом;
- централизует доступ к моделям данных;
- упрощает импорты (достаточно `from app.models import ...`).

Экспортируемые объекты:
- CalendarDay — модель из app.models.calendar.
  Описывает статус дня (рабочий/нерабочий) и название праздника.

Пример использования:
    from app.models import CalendarDay
    day = CalendarDay(date=some_date, is_working=False)

Рекомендации:
- добавляйте в __all__ только публично доступные модели;
- избегайте сложной инициализации — файл должен загружаться быстро;
- при добавлении новых моделей дополняйте список импортов и __all__.
"""

from .calendar import CalendarDay

__all__ = ['CalendarDay']
