"""
WorkCalendarClient — сервис для получения информации о рабочих,
праздничных и выходных днях.

Функциональность:
- Получение данных о днях календаря из внешних источников
  (API, веб‑сайты).
- Нормализация данных к единому формату.
- Кэширование результатов (Redis) для повышения
  производительности.
- Отказоустойчивость: использование резервных источников
  при недоступности основных.
- Предоставление данных через REST API в формате JSON.

Основные компоненты:
- FastAPI — веб‑фреймворк для построения API.
- HTTPX — асинхронный HTTP‑клиент для запросов
  к внешним сервисам.
- BeautifulSoup4 — библиотека для парсинга веб‑страниц.
- Redis — система кэширования.
- PostgreSQL/SQLite — хранилище для долговременных данных.

Экспорт:
- `app` — основной экземпляр приложения FastAPI,
  готовый к запуску.

Использование:
После импорта модуля доступен экземпляр `app`, который можно
использовать с ASGI‑сервером (например, uvicorn)
для запуска сервиса.

Пример запуска:
    uvicorn app:app --host 0.0.0.0 --port 8000
"""

from app.core import settings
from .main import app

__all__ = ['app', 'settings']
