# Пример файла окружения для WorkCalendarClient
#
# Конфигурация разделена по средам:
# - .env.development: настройки для локальной разработки (локальные API, отладочные флаги).
# - .env.testing: параметры для тестового окружения (тестовые API, моки).
# - .env.production: продакшен‑настройки (боевые API, оптимизированные флаги).
#
# Порядок настройки:
# 1. Решите, для какой среды вы настраиваете приложение.
# 2. Создайте копию этого файла с именем .env.<среда> (например, .env.production).
# 3. Внесите в новый файл все необходимые значения переменных.
# 4. В этом (или любом) файле убедитесь, что ENVIRONMENT соответствует выбранной среде.
#
# Примечание: приложение читает переменные из файла, имя которого совпадает с текущим значением ENVIRONMENT.

# --- Среда выполнения ---
# Определяет текущую среду, в которой работает приложение.
# Влияет на то, какие дополнительные настройки будут загружены.
# Допустимые значения:
#   - development — локальная разработка (включена отладка, логирование на максимум);
#   - testing — тестовая среда (используются моки, тестовые API);
#   - production — боевая среда (оптимизированная производительность, минимум логов).
# Если значение не задано, по умолчанию используется "development".
ENVIRONMENT=development|testing|production

# --- Данные ---
# URL для подключения к базе данных в формате:
# dialect+driver://username:password@host:port/database
#
# Параметры:
# - dialect — СУБД (например, postgresql, mysql, sqlite, oracle, mssql)
# - driver — используемый драйвер (например, psycopg2, pymysql, pysqlite)
# - username — имя пользователя для аутентификации
# - password — пароль пользователя (рекомендуется не хранить в открытом виде в конфиге)
# - host — адрес сервера БД (localhost, IP или доменное имя)
# - port — порт сервера БД (по умолчанию: 5432 для PostgreSQL, 3306 для MySQL и т. д.)
# - database — название целевой базы данных
DATABASE_URL=dialect+driver://username:password@host:port/database

# --- Кодировка ---
# Кодировка, используемая для взаимодействия с базой данных и обработки текстовых данных.
# Рекомендуется использовать utf-8 для поддержки всех языков и символов.
ENCODING=utf-8

# --- Настройки логирования ---
# Директория для хранения файлов логов приложения.
# Путь может быть абсолютным или относительным (относительно корня проекта).
# Пример: /var/log/workcalendar/ или ./logs/
LOG_DIR=path/to/log_dir

# Максимальный размер одного файла лога в байтах, после которого происходит ротация.
# Значение по умолчанию: 1 000 000 байт (≈1 МБ).
# При достижении лимита текущий файл закрывается, создаётся новый.
LOG_MAX_BYTES=1000000

# Количество резервных копий лог‑файлов, которые следует хранить.
# При ротации старые файлы переименовываются (log.1, log.2 и т. д.).
# Когда количество достигает указанного лимита, самый старый файл удаляется.
# Допустимые значения: 0 (не хранить архивы), 5, 10 и т. д.
LOG_BACKUP_COUNT=0|5|10

# --- Директория шаблонов ---
# Путь к директории, содержащей шаблоны (например, HTML, email‑шаблоны, отчёты).
# Приложение будет искать шаблоны именно в этой папке.
# Путь может быть:
#   - абсолютным (начиная с корня файловой системы);
#   - относительным (относительно корневой директории проекта).
# Примеры:
#   - /var/www/workcalendar/templates/
#   - ./templates/
#   - ../shared-templates/
# Убедитесь, что указанная директория существует и доступна для чтения приложением.
TEMPLATES_DIR=path/to/templates
